services:
  app:
    build:
      context: ..
      dockerfile: docker.prod/Dockerfile

    env_file:
      - ../.env

    container_name: sb-canada-dict

    ports:
      - "5001:5001"

    volumes:
      - ../:/app

    restart: unless-stopped

    networks:
      - sb-canada-dict

networks:
  sb-canada-dict:
